import React from 'react';
import PropTypes from 'prop-types';

import styles from './styles.scss';

const Tale = props => (
   <div>
      Tale
   </div>
)

Tale.propTypes = {

}

export default Tale
